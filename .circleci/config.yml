version: 2
jobs:
  build:
    docker:
      - image: python:2.7
    working_directory: /home/lmswebaula/
    steps:
      - checkout
      - run:
          name: Create test folders
          command: |
            mkdir -p /home/lmswebaula/test-reports/coverage
      - run:
          name: Install packate
          command: |
            python /home/lmswebaula/setup.py develop
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements-dev.txt" }}
      - run:
          name: Install Requirements
          command: |
            pip install -r requirements-dev.txt
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements-dev.txt" }}
          paths:
            - "/home/lmswebaula/.pip-cache"
      - run:
          name: Run tests
          command: |
            pytest --cov-config .coveragerc --cov=lms /home/lmswebaula/tests/

      - store_artifacts:
          path: test-reports/coverage
          destination: reports
      - store_test_results:
          path: "test-reports/"