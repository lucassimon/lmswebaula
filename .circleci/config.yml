version: 2
jobs:
  build:
    docker:
      - image: python:2.7
    working_directory: /home/lmswebaula/
    steps:
      - checkout
      - run:
          name: Create test folders
          command: |
            mkdir -p /home/lmswebaula/reports/coverage
      - run:
          name: Install package
          command: |
            python /home/lmswebaula/setup.py develop
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements-dev.txt" }}
      - run:
          name: Install Requirements
          command: |
            pip install -r requirements-dev.txt
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements-dev.txt" }}
          paths:
            - "/home/lmswebaula/.pip-cache"
      - run:
          name: Run tests
          command: |
            pytest --cov-config /home/lmswebaula/.coveragerc --cov=lms /home/lmswebaula/tests/

      - store_artifacts:
          path: /home/lmswebaula/reports/coverage
      - store_test_results:
          path: "/home/lmswebaula/.coverage"